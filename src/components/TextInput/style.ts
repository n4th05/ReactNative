import { StyleSheet  } from "react-native";

export const styles = StyleSheet.create({
  input:{
    backgroundColor: "#353535",
    width: 350,
    height: 55,
    borderRadius: 5,
    paddingLeft: 10,
    fontSize: 15,
    borderColor: "#ccc",
    borderWidth: 1,
    gap: 10
  }
});